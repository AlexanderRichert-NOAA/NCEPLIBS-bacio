name: Validate PR

on:
  pull_request:
    types: [opened, synchronize, reopened, review_requested, edited]

jobs:
  Checks:
    permissions:
      pull-requests: read
    uses: AlexanderRichert-NOAA/ci-validate-pr/.github/workflows/validate-pr.yaml@638d8a528347ea9f90d0cec838d465169470591d
    with:
      validate-genai-disclosure: true
      approvals-for-external-contributors: 1
