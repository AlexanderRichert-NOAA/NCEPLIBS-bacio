name: Validate PR

on:
  pull_request:
    types: [opened, synchronize, reopened, review_requested, edited]

jobs:
  Checks:
    permissions:
      pull-requests: read
    uses: AlexanderRichert-NOAA/ci-validate-pr/.github/workflows/validate-pr.yaml@6ff858c895f6703ff245e9fb3d86caeb3baa811a
    with:
      validate-genai-disclosure: true
      approvals-for-external-contributors: 1
